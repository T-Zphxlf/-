<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Voice input permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.NETWORK" />
    <!-- For dictionary updates -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- TV feature declarations -->
    <uses-feature android:name="android.hardware.type.tv" android:required="false" />
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="true" />
    <!-- Only arm64-v8a -->
    <uses-feature android:name="android.hardware.type.tv" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/VoiceTypeTheme"
        android:banner="@mipmap/ic_banner"
        tools:targetApi="34">

        <!-- Input Method Service -->
        <service
            android:name=".ime.VoiceTypeIME"
            android:label="@string/ime_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- Settings Activity -->
        <activity
            android:name=".ime.settings.SettingsActivity"
            android:label="@string/settings_title"
            android:exported="true"
            android:theme="@style/VoiceTypeTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Voice Recognition Activity (transparent, for permission handling) -->
        <activity
            android:name=".ime.voice.VoicePermissionActivity"
            android:label="@string/voice_permission_title"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:exported="false" />

    </application>

</manifest>
